#include <iostream>
using namespace std;

string* moreLong(string s1, string s1){
	string s[2];
	if(s1. >= s2.length){
		return s;
	}
	return s;
}

string translate(string str, int length){
	while(str.length() != length){
		str = "0" + str;
	}
	return str;
}

int main() {
    int a,b,c,d;
    scanf("%d %d %d %d",&a, &b, &c, &d);



    string ab = to_string(a) + to_string(b);
    cout << ab << endl;
    string cd = to_string(c) + to_string(d);
    cout << cd << endl;
    cout << atoi(ab.c_str()) + atoi(cd.c_str()) << endl;

    return 0;
}
